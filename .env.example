# .env.example

# InfluxDB (read-only queries)

# Hostname or service name reachable from *inside* the Docker network
# SAME AS SET IN SWAG
INFLUX_HOST=influxdb

# InfluxDB HTTP API port *inside* the Docker network (usually 8086)
# SAME AS SET IN SWAG
INFLUX_HOST_PORT=8086

# InfluxDB database that contains the geoip2influx measurement
# SAME AS SET IN SWAG
INFLUX_DATABASE=geoip2influx

# Optional (leave blank if auth disabled)
# SAME AS SET IN SWAG
INFLUX_USER=influxer

# Read password from a mounted secret file
# SAME AS SET IN SWAG
INFLUX_PASS_FILE=/run/secrets/influxdbv1_pass

# Compatibility: allow direct password env var
# SAME AS SET IN SWAG
# INFLUX_PASS=changeme

# Measurement name for GeoIP points
# SAME AS SET IN SWAG
GEO_MEASUREMENT=geoip2influx


# Heatmap rendering behavior

# Query window (Influx duration), e.g. 1h, 24h, 7d
HEATMAP_TIME_WINDOW=24h

# How often the browser refreshes the map data (seconds)
HEATMAP_REFRESH_SECONDS=30

# Server-side cache TTL for /data responses (seconds)
HEATMAP_CACHE_SECONDS=30

# Optional safety cap on returned points (0 = unlimited)
HEATMAP_MAX_POINTS=20000

# Optional title (leave blank/omit for none)
# HEATMAP_TITLE=GeoIP Heatmap
