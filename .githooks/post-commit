#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "Building post-commit LLM archive..."

.scripts/build-context-archive.sh

echo "Post-commit archive complete."
